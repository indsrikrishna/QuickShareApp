chmod +x ./gradlewname: Build Android APK
chmod +x ./gradlew
chmod +x ./gradlewon:
chmod +x ./gradlew  push:
chmod +x ./gradlew    branches: [ main ]
chmod +x ./gradlew
chmod +x ./gradlewjobs:
chmod +x ./gradlew  build:
chmod +x ./gradlew    runs-on: ubuntu-latest
chmod +x ./gradlew
chmod +x ./gradlew    steps:
chmod +x ./gradlew      - name: Checkout
chmod +x ./gradlew        uses: actions/checkout@v4
chmod +x ./gradlew
chmod +x ./gradlew      - name: Set up JDK 17
chmod +x ./gradlew        uses: actions/setup-java@v4
chmod +x ./gradlew        with:
chmod +x ./gradlew          java-version: '17'
chmod +x ./gradlew
chmod +x ./gradlew      - name: Install Android SDK (basic)
chmod +x ./gradlew        run: |
chmod +x ./gradlew          sudo apt-get update
chmod +x ./gradlew          sudo apt-get install -y unzip
chmod +x ./gradlew          sdkmanager --install "platform-tools" "platforms;android-33" || true
chmod +x ./gradlew
chmod +x ./gradlew      - name: Make gradlew executable
chmod +x ./gradlew        run: chmod +x ./gradlew ||
chmod +x ./gradlew
chmod +x ./gradlewmkdir -p .github/workflows && cat > .github/workflows/build-apk.yml <<'YAML'
chmod +x ./gradlewname: Build Android APK
chmod +x ./gradlew
chmod +x ./gradlewon:
chmod +x ./gradlew  push:
chmod +x ./gradlew    branches: [ main ]
chmod +x ./gradlew
chmod +x ./gradlewjobs:
chmod +x ./gradlew  build:
chmod +x ./gradlew    runs-on: ubuntu-latest
chmod +x ./gradlew
chmod +x ./gradlew    steps:
chmod +x ./gradlew      - name: Checkout
chmod +x ./gradlew        uses: actions/checkout@v4
chmod +x ./gradlew
chmod +x ./gradlew      - name: Set up JDK 17
chmod +x ./gradlew        uses: actions/setup-java@v4
chmod +x ./gradlew        with:
chmod +x ./gradlew          java-version: '17'
chmod +x ./gradlew
chmod +x ./gradlew      - name: Install Android SDK (basic)
chmod +x ./gradlew        run: |
chmod +x ./gradlew          sudo apt-get update
chmod +x ./gradlew          sudo apt-get install -y unzip
chmod +x ./gradlew          sdkmanager --install "platform-tools" "platforms;android-33" || true
chmod +x ./gradlew
chmod +x ./gradlew      - name: Make gradlew executable
chmod +x ./gradlew        run: chmod +x ./gradlew || true
chmod +x ./gradlew
chmod +x ./gradlew      - name: Build debug APK
chmod +x ./gradlew        run: ./gradlew :app:assembleDebug --no-daemon
chmod +x ./gradlew
chmod +x ./gradlew      - name: Upload APK
chmod +x ./gradlew        uses: actions/upload-artifact@v4
chmod +x ./gradlew        with:
chmod +x ./gradlew          name: app-debug-apk
chmod +x ./gradlew          path: app/build/outputs/**/*.apk
