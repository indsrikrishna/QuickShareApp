name: Build Android APK

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'

      - name: Install Android SDK (basic)
        run: |
          sudo apt-get update
          sudo apt-get install -y unzip
          sdkmanager --install "platform-tools" "platforms;android-33" || true

      - name: Make gradlew executable
        run: chmod +x ./gradlew ||

mkdir -p .github/workflows && cat > .github/workflows/build-apk.yml <<'YAML'
name: Build Android APK

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'

      - name: Install Android SDK (basic)
        run: |
          sudo apt-get update
          sudo apt-get install -y unzip
          sdkmanager --install "platform-tools" "platforms;android-33" || true

      - name: Make gradlew executable
        run: chmod +x ./gradlew || true

      - name: Build debug APK
        run: ./gradlew :app:assembleDebug --no-daemon

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: app-debug-apk
          path: app/build/outputs/**/*.apk
